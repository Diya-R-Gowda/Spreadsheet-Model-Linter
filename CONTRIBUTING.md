# Contributing to Spreadsheet Model Linter

This file tracks what's actually built versus what's still on the plan, week by week, plus the architecture and tech-stack decisions a contributor needs to know before touching the code. If you're picking this project back up after a break, start here, not the README — the README sells the idea, this file tells you where the code actually stands.

**Legend:** 🟢 done and tested · 🔴 not started

---

## Dev setup

```bash
# from the repo root
python -m venv .venv
./.venv/Scripts/activate        # Windows; use `source .venv/bin/activate` on macOS/Linux
pip install -e ".[dev]"

pytest                          # regenerates synthetic fixture workbooks automatically
ssmlint dump path/to/workbook.xlsx
```

Project layout:
```
src/ssmlint/
  parser.py          # [1] workbook parsing spine
  cli.py              # `ssmlint dump` / `ssmlint report` entry points
  tokenizer.py         # [2] formula lexer
  formula_parser.py    # [2] formula AST builder
  ast_nodes.py          # [2] AST node dataclasses
  r1c1.py                # [3] position-independent formula normalization
  errors.py                # shared FormulaError hierarchy
  depgraph.py               # [4] workbook-wide precedent/dependent DAG
  blocks.py                  # [5] row-wise block clustering + near-miss detection
  rules/                      # [6] Tier 0 rule engine
    base.py                    # Issue dataclass, Rule base interface
    literal_in_block.py         # literal-in-formula-block rule
    range_boundary.py            # range-boundary-mismatch rule
    reference_to_blank.py         # reference-to-blank rule
    inconsistent_anchoring.py      # inconsistent-anchoring rule
  evaluation.py                 # Week 4: TP/FP/FN/known_intentional scoring logic, reused by Week 5/6
  labeling.py                    # Week 5: block-level 4-way label generation + split (plumbing only, no ML deps)
  ablation.py                     # Week 6: [Configuration, Precision, Recall, Precision@10, Cost, Runtime] table
  report.py                        # Week 6: single-workbook ranked JSON + HTML report builder
  templates/report.html.jinja       # Week 6: the single self-contained Jinja2 template
scripts/generate_fixtures.py   # builds synthetic .xlsx test fixtures
scripts/generate_corpus.py      # builds the synthetic-corruption evaluation/training corpus
scripts/run_evaluation.py        # Week 4 CLI: runs the rule engine against corpus/, prints the scored report
scripts/run_labeling.py           # Week 5 CLI: prints the label-generation/training-readiness report
scripts/run_ablation.py            # Week 6 CLI: prints the ablation table
tests/                          # pytest suite; fixtures regenerated per session, not committed
corpus/                          # synthetic-corruption corpus (.xlsx + paired ground-truth JSON); generated on demand, not committed
diya.md                          # the approved Week 6 implementation plan, saved verbatim before implementation started
```

---

## Architecture

Nine-stage pipeline, each stage consuming the previous stage's output:

```
.xlsx file
   │
   ▼
[1] Workbook Parser ─────────► cells, formulas, formats, named ranges       🟢
   │                            (openpyxl, data_only=False)
   ▼
[2] Formula Tokenizer/AST ───► hand-rolled lexer + recursive-descent parser  🟢
   │                            (functions, refs, literals, operators)
   ▼
[3] R1C1 Normalizer ─────────► relative formulas → position-independent    🟢
   │                            strings (the heart of the project)
   ▼
[4] Dependency Graph ────────► DAG of cell → precedents/dependents          🟢
   │                            (networkx)
   ▼
[5] Block Detector ──────────► contiguous regions with shared structure    🟢
   │                            (R1C1 equality + rapidfuzz for near-misses)
   ▼
[6] Rule Engine (Tier 0) ────► deterministic checks, always on, $0         🟢
   │
   ▼
[7] Trained Classifier (Tier 1, optional) ─► subtotal | intentional_override | 🔴
   │                            suspected_error | unknown
   │                            small encoder, CPU-only, trained on synthetic
   │                            corruption labels
   ▼
[8] Local LLM Adjudicator (Tier 2, optional, flag-gated) ─► same 4-way label 🔴
   │                            3B model via Ollama, grammar-constrained
   ▼
[9] Report Builder ──────────► ranked JSON + standalone HTML                🟢 (Tier 0 only)
```

**Design principle:** everything downstream of stage [2] consumes the AST, never raw formula strings or raw cell dumps. Any semantic layer (Tier 1 or Tier 2) sees a compact normalized block description, never a pasted sheet.

**Zero-cost, offline-first:** no hosted API is a required dependency anywhere in this pipeline. Tier 0 (rules) and Tier 1 (trained classifier) are both $0 and CPU-only by construction; Tier 2 (local LLM) is optional and flag-gated, capped at a 3B model — sized for the target dev machine (13th-gen Intel i5-1335U, 16GB RAM, Iris Xe iGPU, no dedicated VRAM), which is memory-bandwidth-bound and rules out 7B+ models for interactive use.

### Tech stack

| Layer | Choice | Why |
|---|---|---|
| Parsing | `openpyxl` | Only mature lib exposing raw formulas |
| Formula AST | hand-rolled tokenizer/parser | Full control, no third-party formula-parsing dependency |
| R1C1 normalization | hand-rolled | Core transformation of the project |
| Graph | `networkx` | Cycle detection, topological ordering |
| Clustering | R1C1 equality + `rapidfuzz` | Exact match covers most; fuzzy catches near-misses |
| Semantic layer — Tier 1 (planned) | DeBERTa-v3-small / ModernBERT-base, fine-tuned on synthetic-corruption labels | CPU-only, ms/block, $0 |
| Semantic layer — Tier 2 (planned, optional) | Local 3B model via Ollama (Qwen2.5-3B-Instruct or Phi-3-mini) | Fits in 16GB RAM; 7B+ excluded for latency reasons |
| Report | Jinja2 → single-file HTML | Must open without a server |
| Tests | `pytest` + generated fixture workbooks | Fixtures regenerated per session, not committed as binaries |

---

## Progress Tracker

### Week 1 — Parsing spine

| Task | Status | Detail |
|---|:---:|---|
| Workbook parser (`parser.py`) | 🟢 | Loads `.xlsx`/`.xlsm` via `openpyxl` with `data_only=False` so raw formula strings are captured, never Excel's cached computed values. Per cell: sheet-qualified address (`Sheet1!C14`), formula or literal value, number format string, merged-range membership, and cross-sheet reference detection. |
| Named range resolution | 🟢 | Resolves both workbook-scoped (`wb.defined_names`) and sheet-scoped (`ws.defined_names`, distinguished by `localSheetId`) named ranges. |
| Skip-and-log for unparseable cells | 🟢 | Array formulas (`ArrayFormula` objects), `LET`/`LAMBDA`, and structured table references are detected and logged to a `skipped` list with cell address + reason, rather than crashing the parse. A catch-all wraps every cell so one malformed cell can't abort the whole workbook. |
| CLI (`ssmlint dump`) | 🟢 | `argparse`-based, `dump` subcommand, JSON to stdout or to a file via `-o`. Installed as a console script (`ssmlint`). |
| Synthetic test fixtures | 🟢 | `scripts/generate_fixtures.py` builds 4 workbooks (clean formula row, merged cells, named ranges + cross-sheet formula, unparseable-formula edge cases) programmatically — regenerated every `pytest` session via `conftest.py`, not committed as binaries, so they can never drift out of sync with the generator. |
| Pytest suite | 🟢 | 11 tests: formula extraction, literals, merged cells, named ranges, cross-sheet detection, skip-and-log behavior, unsupported file extensions, JSON shape, CLI stdout/file output. |

**How it was solved:** Built directly on `openpyxl`'s cell/worksheet model. The trickiest part was merged cells — only the top-left cell of a merged range holds a real value in openpyxl; the rest are `MergedCell` instances with `None` values but still readable `number_format` — handled by checking `coordinate in merged_range` for every cell rather than relying on cell identity. Named ranges needed two different lookup paths (workbook dict vs. per-worksheet dict) since openpyxl doesn't unify them.

**Bugs/defects encountered:** None. The parser and CLI passed all 11 tests on first full run and were verified end-to-end against the installed console script before being marked done.

---

### Week 2 — R1C1 normalization + dependency graph

The README's original Build Plan bundled "R1C1 normalization + dependency graph" into one week, but building the AST they both depend on turned out to be substantial enough to track as its own row.

| Task | Status | Detail |
|---|:---:|---|
| Formula tokenizer (`tokenizer.py`) | 🟢 | Hand-rolled lexer, no third-party formula-parsing library. Single ordered master regex splits a formula string into cell references (with independent `$` anchoring per axis), sheet-qualified refs (bare and quoted, e.g. `'My Sheet'!A1`), named ranges, function names, all Excel operators, parens, commas, and string/number/boolean literals. |
| Formula AST parser (`formula_parser.py`) | 🟢 | Recursive-descent parser with correct Excel operator precedence and associativity: `^` before `* /` before `+ -` before comparisons, string concat with `&`, and Excel's specific unary-minus-before-`^` rule. Builds `CellRef` / `RangeRef` / `NamedRangeRef` / `Literal` / `FunctionCall` / `BinaryOp` / `UnaryOp` nodes (`ast_nodes.py`). |
| Whitelist enforcement | 🟢 | Array formulas, `LET`/`LAMBDA`, structured table references, full-column/full-row ranges (`A:A`, `1:1`, including `$`-anchored forms like `$A:$B`), 3-D sheet ranges, and the percent operator all raise `UnsupportedFormulaError` cleanly. Genuinely malformed input raises a separate `FormulaSyntaxError`. Both share a common `FormulaError` base so callers can catch broadly or narrowly. |
| Standalone/independently testable | 🟢 | Not wired into `parser.py` yet, by design — this stage is testable in isolation before the pipeline commits to using it. |
| R1C1 Normalizer (`r1c1.py`) | 🟢 | Converts a formula's AST plus its origin cell address into a position-independent R1C1-style string. Each `CellRef` axis becomes a bracketed relative offset (`R[1]C[-1]`) if unanchored, or an unbracketed absolute position (`R14C2`) if `$`-anchored — independently per axis, so `$B14` and `B$14` normalize differently from each other and from `B14`. `RangeRef` normalizes both endpoints; cross-sheet refs get a `Sheet!` prefix kept structurally separate from the offset part; `NamedRangeRef` passes through unresolved (case preserved — unlike function names, named ranges are case-sensitive in Excel); function names are upper-cased; every `BinaryOp`/`UnaryOp` is explicitly parenthesized in the output so structurally different ASTs can never coincidentally flatten to the same string. |
| Dependency Graph (`depgraph.py`) | 🟢 | Workbook-wide `networkx.DiGraph` where nodes are sheet-qualified addresses and an edge `precedent -> dependent` means "dependent's formula reads from precedent" — chosen specifically so `topological_sort()` doubles as a valid calculation order with no reversal. Walks every formula's AST, expands `RangeRef`s to individual cell edges (a `SUM(B4:B10)` is 7 edges, never one range-shaped edge), resolves `NamedRangeRef`s against `parser.py`'s captured named ranges (sheet-scoped names shadow workbook-scoped ones, matching Excel), and cross-sheet refs become ordinary edges into the target sheet. `precedents()`/`dependents()`/`find_cycles()`/`topological_order()` query methods; the last raises a dedicated `CyclicDependencyError` (carrying the offending cycles) instead of leaking networkx's own exception. Every node also carries an `empty` attribute (computed once, not re-derived per query) and a `parse_error` attribute so cells with unparseable or catastrophically-failed formulas still exist as nodes with no outgoing edges rather than vanishing. |

**Formula tokenizer/parser — how it was solved:** The lexer uses one big alternation-based regex tried in a fixed order (multi-char operators like `<=` before their single-char prefixes; sheet-prefix and cell-address patterns before the generic identifier pattern, since identifiers would otherwise swallow them whole). The parser is a standard precedence-climbing recursive descent, with one deliberate deviation from "normal" language grammars: Excel evaluates `-2^2` as `4`, not `-4` — unary minus binds *tighter* than `^`, the opposite of Python/most languages — so `unary` sits *below* `power` in the grammar instead of wrapping it. `^` itself is left-associative in Excel (`2^3^2 = 64`, not `512`), confirmed with dedicated tests. A small literal-only AST evaluator was written in the test suite specifically to pin down these precedence/associativity cases numerically rather than just asserting tree shape.

**Formula tokenizer/parser — bugs/defects encountered and fixed:**
1. **`$`-anchored full-column/row ranges crashed instead of being rejected cleanly.** Formulas like `VLOOKUP(A1,Sheet2!$A:$B,2,FALSE)` — common in real `VLOOKUP`/`SUMIFS` usage — raised a raw `"unexpected character '$'"` `FormulaSyntaxError` instead of a proper `UnsupportedFormulaError`, because no token pattern consumed a `$` that wasn't immediately followed by row digits. Found during manual spot-checking with realistic formulas beyond the adversarial test suite (the test suite itself hadn't covered this shape). Fixed by adding dedicated `COL_ONLY`/`ROW_ONLY` token kinds and routing them through the same rejection path as plain `A:A`/`1:1` ranges. Added regression tests (`test_rejects_anchored_full_column_range`, `test_rejects_anchored_full_row_range`).
2. **Inconsistent error type for a stray `:` depending on where it appeared.** A colon inside function-call arguments (e.g. `SUM(1:1)`) raised a generic `FormulaSyntaxError` via the parser's `_expect(RPAREN)` check, while the same construct at the top level correctly raised `UnsupportedFormulaError`. Caught while writing the rejection tests, before it shipped. Fixed by centralizing all "unexpected token" handling into one `_raise_unexpected` helper that every mismatch path now calls, so a stray `:` is always classified as a whitelist rejection regardless of where the parser encounters it.

**R1C1 normalizer — how it was solved:** Every `CellRef` axis is formatted independently by its own `$`-anchoring flag: relative → `[delta]` where `delta = target - origin`; absolute → the bare literal position, ignoring the origin entirely. Cross-sheet handling falls out of the API design for free — `normalize()` only ever receives the *origin cell's address* (e.g. `"C14"`), never its sheet — so a formula's own sheet can't influence the output, which is exactly what makes "same relative shape, different host sheet, same explicit cross-sheet target" normalize identically without any special-case code. Verified with a manual audit printing actual before/after strings for 4 required pairs (all matched expectations) before being marked done, not just a green pytest run.

**R1C1 normalizer — bugs/defects encountered and fixed:**
1. **Test suite asserted the wrong expected value for named ranges.** Two tests initially expected `TaxRate` to normalize to `TAXRATE` (upper-cased, mirroring the function-name rule), and failed. Root cause was the test's assumption, not the implementation: Excel function names are case-insensitive but named-range names are case-sensitive, and `r1c1.py` already preserved the original case correctly per spec ("passes through as the name itself"). Fixed by correcting the two test expectations (`test_named_range_passes_through_unresolved`, `test_sheet_scoped_named_range_is_prefixed`) to preserve case rather than changing the implementation.

**Known, documented limitation (not a bug):** a formula that explicitly writes its own sheet name (e.g. `=Sheet1!B14*C14` while physically living on `Sheet1`) is treated as cross-sheet and will *not* normalize identically to the equivalent implicit form `=B14*C14`, because `normalize()`'s API deliberately doesn't take the origin's sheet name as an argument (only its row/column). Fixing this would require widening the public API; documented in `r1c1.py`'s module docstring rather than silently mishandled.

**Dependency graph — how it was solved:** Two design decisions did most of the work. First, the edge direction (`precedent -> dependent`) was picked specifically so `networkx.topological_sort()` needs no reversal to produce a valid Excel calculation order, and so `precedents(cell)`/`dependents(cell)` map directly onto `predecessors()`/`successors()` with no adapter logic. Second, rather than trusting `parser.py`'s own `skipped` log to decide which cells get no outgoing edges, this module re-parses every formula itself with `formula_parser.parse_formula()` — the two layers don't always agree (see the array-formula case below), so re-parsing is the more accurate source of truth. Named-range resolution reuses `formula_parser.parse_formula()` a second time, on the named range's own `refers_to` text, instead of writing a second reference-parsing grammar. The "is this cell empty" question was deliberately made a node attribute computed once at build time rather than a live lookup against `ParsedWorkbook`, since the rule engine (next stage) will ask it repeatedly per edge and the answer can never change after the graph is built. Verified with hand-built `ParsedWorkbook` objects for the cases a real `.xlsx` can't easily produce (circular refs, dangling named ranges, a cell that never got a `CellRecord` at all), plus real-fixture integration tests, plus manual spot-checks beyond the test suite (self-loops, 3-node cycles, duplicate refs in one formula, reversed ranges like `B10:B4`, and a formula combining a range + a named range + a cross-sheet ref in one expression) — all matched expectations with no further bugs found.

**Dependency graph — bugs/defects encountered and fixed:** None. Every hand-built test case, every fixture integration test, and every manual spot-check (including several scenarios beyond the required edge cases — self-loops, 3-node cycles, duplicate same-cell references, reversed range bounds) passed on first implementation.

---

### Week 3 — Block detection + rule engine

| Task | Status | Detail |
|---|:---:|---|
| Block detector (`blocks.py`) | 🟢 | Clusters each sheet's rows into blocks of contiguous cells sharing one exact R1C1 pattern (row-wise only — see "how it was solved"). A gap of even one non-conforming cell ends a block; nothing is ever merged across it. For each block, the immediate left/right neighbor is classified as either `non_conforming` (blank, literal, unparseable, or a formula scoring below the near-miss threshold) or `near_miss` (a different-but-similar formula, via `rapidfuzz.fuzz.ratio`, with its score attached). Minimum block size is 3 cells. |
| Rule engine — literal-in-formula-block (`rules/literal_in_block.py`) | 🟢 | Flags a literal touching a block's `non_conforming` list (blocks.py's own output — no new traversal). Severity is exactly two levels from structure alone: `high` when the literal is bordered by blocks on *both* sides sharing the identical pattern (the README's own H14 shape — one continuous pattern with a single interruption), `medium` for everything else this rule still flags (bordered on one side only, or two different patterns on each side). Always labeled `suspected_error`, never `intentional_override` — that judgment is explicitly deferred to Tier 1/2. `Issue` dataclass (`cell`, `severity`, `rule_id`, `explanation`, `suggested_fix`) and a `Rule` ABC live in the new `rules/base.py`, shared by every future Tier 0 rule. |
| Rule engine — range boundary mismatch (`rules/range_boundary.py`) | 🟢 | Flags a formula whose range argument (`SUM`/`AVERAGE`/bare range/etc.) has a different span than its block-mates', even though the rest of the formula matches. Inspects both `non_conforming` and `near_misses` (confirmed by testing 7 constructed cases that a genuine off-by-one lands in `near_misses`, not `non_conforming` — but doesn't trust that bucketing, since it's a rapidfuzz-score artifact, not a guarantee) and independently verifies via R1C1 string structure that exactly one range boundary shifted by exactly 1, with everything else in the formula identical. Same two-level severity scheme as `literal_in_block` (`high`: bordered by same-pattern blocks on both sides; `medium`: otherwise), reused deliberately rather than inventing a new member-count threshold. |
| Rule engine — reference to blank (`rules/reference_to_blank.py`) | 🟢 | Flags a block member's formula that reads from a genuinely blank precedent (`DependencyGraph.is_empty()`), but only when at least one sibling in the same block (sharing the identical R1C1 pattern) has a real, populated precedent at the equivalent position — the one case investigation found real deterministic evidence for. Isolated non-block-member references and blocks where every member uniformly references the same blank cell are never flagged (no signal for either). Severity: `high` when clean members outnumber affected ones, `medium` otherwise. Required widening `rules/base.py`'s `Rule.evaluate()` to accept an optional `graph` parameter — confirmed backward compatible (full suite re-run in isolation before building on top of it). Backed by both hand-built `ParsedWorkbook` scenarios and a real fixture (`reference_to_blank_gap.xlsx`) through the full pipeline. |
| Rule engine — inconsistent anchoring (`rules/inconsistent_anchoring.py`) | 🟢 | Flags a formula whose `$`-anchoring on a specific axis (row or column) breaks from what the rest of a block establishes for that same logical reference — e.g. every member holds a shared growth-rate cell fixed as `$B$1`, one drifts to `B1`/`$B1`/`B$1`. Detects per-axis via R1C1 token structure: a differing reference is only flagged when it resolves to the *same* real target cell (via the candidate's own address as origin) with different anchoring — a reference to a genuinely different cell is left alone. Confirmed structurally mutually exclusive from `range_boundary`'s territory (shifted boundary = different cell; this rule = same cell, different anchoring) with an explicit cross-firing test. Same two-level severity scheme as every prior rule, reused deliberately. |
| Synthetic-corruption corpus (build now, not later) | 🟢 | `scripts/generate_corpus.py` builds a 47-entry corpus (94 files: paired `.xlsx` + `.ground_truth.json`) from two parametrized base shapes (growth-chain row, trailing-window-sum row) plus one cross-block-isolation fixture (two independent blocks on one sheet), covering all four Tier 0 rules with controllable severity via injection position. Regenerated on demand into `corpus/` (gitignored, same as `tests/fixtures/`), never committed as binaries. |

**Block detector — how it was solved:** Clusters along rows only, not columns — the README's own worked example (`C14:N14`, a revenue row with a month per column) is row-wise, and it's the documented primary shape for these models; column-wise clustering was deliberately left out of scope for this stage rather than half-built. Two judgment calls without a derivable "correct" answer were made explicit rather than picked silently: **minimum block size = 3** (2 adjacent same-pattern cells is weak evidence of an intentional pattern — could easily be coincidence — while 3+ is a much stronger signal), flagged in the delivery summary; and the **rapidfuzz near-miss threshold = 85** (`fuzz.ratio`, character-level and order-sensitive — deliberately not `token_sort_ratio`, which would wrongly call `R[0]C[-1]-R[0]C[-2]` and `R[0]C[-2]-R[0]C[-1]` identical when they mean different things), which was raised as a direct question rather than folded into the summary, per instruction that a second judgment call this significant should be asked, not assumed. Blank-cell detection defers to `DependencyGraph.is_empty()` when the neighbor is a graph node, reusing `depgraph.py`'s single already-documented definition of "empty" instead of re-deriving it a third time; cells that never became graph nodes (unreferenced literals) fall back to deriving it straight from the `CellRecord`. Verified by printing actual block spans/patterns/deviations (not just pass/fail) for every required scenario — a clean row block, a gap producing two separate blocks, a literal in the middle of what would've been one run, a near-miss with its real computed score (92.59 for a `+1` tacked onto an otherwise-identical formula, 46.34 for a genuinely unrelated one), and the N-1-vs-N minimum-size boundary — plus the full 12-column README-style revenue row fixture (`revenue_row_with_hardcode.xlsx`), which split cleanly into `C14:G14` and `I14:M14` around the hardcoded `H14`, with `H14` correctly reported as a non-conforming neighbor of *both* blocks.

**Block detector — bugs/defects encountered and fixed:** None in the implementation itself. While writing the test suite, three of my own test assertions had hand-computed R1C1 pattern strings with row/column offsets transposed (e.g. asserting `R[-1]C[0]` where the module actually and correctly produces `R[0]C[-1]`) — caught immediately by cross-checking against `r1c1.normalize()`'s real output before trusting the assertion, per the standard set during the R1C1 stage of verifying actual values rather than assuming derivations. Two other early test assertions asserted a block's `non_conforming` list was empty without accounting for a seed literal cell (`B14`) that was always present as that block's left neighbor in the test data — a test-data oversight, not a code bug; fixed by removing the irrelevant seed cell from those two tests so they isolate the one behavior each was meant to check.

**FIXED (post-stage bugfix): the r1c1.py self-sheet-reference limitation.** The limitation described in the original version of this write-up — a formula explicitly spelling out its own sheet name (`=Sheet1!B14*C14` while living on `Sheet1`) normalizing differently from the equivalent implicit form (`=B14*C14`) — was confirmed as a real, reproducible bug during an audit (constructed a `C14:I14` row where `F14` used the explicit self-sheet form; it split into two 3-cell blocks with `F14` excluded from both). Fixed in `r1c1.py`: `normalize()` now takes a required `origin_sheet` parameter and collapses a reference to the origin's own sheet to the same output as the implicit form, leaving genuine cross-sheet references untouched. `blocks.py` was updated to pass each cell's own sheet through (its one call site was the only caller besides `r1c1.py`'s own tests). Regression test added (`test_explicit_self_sheet_reference_does_not_cause_a_false_split` in `test_blocks.py`) confirming the same `C14:I14` construction now produces one 7-cell block with empty `non_conforming`/`near_misses`. Full suite: 110 → 114 passing, zero regressions. `NamedRangeRef` sheet-prefixing was deliberately left untouched — never reported as a bug, out of scope for this fix.

**Known limitation, new to this stage:** a cell that fails so catastrophically in `parser.py`'s per-cell catch-all that it never gets a `CellRecord` at all (only an entry in `ParsedWorkbook.skipped`, plus a bare node in the dependency graph) is invisible to this module's row grouping, since `blocks.py` walks `ParsedWorkbook.sheets[*].cells` directly. Such a cell could never be reported as a block's non-conforming neighbor even though the graph knows about it. Judged acceptable — this requires an `openpyxl` per-cell read to fail outright, which essentially never happens in practice — and documented in `blocks.py`'s module docstring rather than silently patched around with graph-splicing logic no fixture or test currently needs.

**Rule engine (literal-in-formula-block) — how it was solved:** Before writing any rule logic, confirmed by reading `blocks.py::_build_block` (not assuming) exactly what `non_conforming` captures: precisely the one cell immediately left of a block's start column and the one immediately right of its end column, nothing further. Verified live with three constructed scenarios: a single literal fully between two runs (the README's H14 shape) is completely captured by both bordering blocks; two adjacent literals in a gap are each captured by exactly one side; three adjacent literals in a gap leave the *middle* one invisible to every block. Reported this finding and asked before proceeding rather than fixing it as a drive-by inside a completed stage — user chose to ship v1 on `non_conforming` as-is and document the gap, rather than widening `blocks.py`'s traversal now. The rule itself groups a literal's sightings across up to two bordering blocks (a cell has at most two sides) into one `Issue`, never a duplicate. Severity uses only structural data blocks.py actually provides (no computed-magnitude signal exists, since cells are never recalculated) — deliberately kept to two levels rather than adding a third/fourth tier that would need its own unjustified numeric threshold, consistent with how `min_block_size` and the rapidfuzz threshold were handled in the previous stage. The "is this a total/subtotal row" intentional-override heuristic was deliberately **not** built — there's no reliable signal for it at this stage, and the README itself names `intentional_override` as a Tier 1/2 label, not Tier 0's to guess at. Verified against the real `revenue_row_with_hardcode.xlsx` fixture through the full pipeline (not a hand-built stub): `H14` correctly comes back `high` with both bordering blocks' spans and the shared pattern named in the explanation; `B14` (the growth chain's seed literal) also fires, at `medium` — a real, expected precision consequence of flagging conservatively by default, not a bug, since the seed of a series is structurally indistinguishable from an accidental hardcode at Tier 0.

**Rule engine (literal-in-formula-block) — bugs/defects encountered and fixed:** None in the implementation. One test-construction mistake caught before it shipped: an early version of the "clean block, zero issues" test used formulas like `=Z13+1` intending "the cell diagonally up-left," which doesn't exist for a block starting at column A — caught by printing the actual block/pattern output first (per this project's standing verification habit) rather than trusting the hand-written formula, and fixed by using a same-column-previous-row pattern (`=A13+1`, `=B13+1`, `=C13+1`) instead.

**Known limitation, confirmed and deferred (not fixed) in this stage:** a literal sitting in the interior of a run of 3+ contiguous non-conforming cells between two blocks is invisible to `non_conforming` entirely, so `literal-in-formula-block` cannot flag it either — reported to the user before writing the rule; ship-as-is was the explicit choice, to be revisited only if a future stage needs it, not patched into `blocks.py` as a side effect of this one rule.

**Rule engine (range-boundary-mismatch) — how it was solved:** Before writing any rule logic, worked out — then verified, not just derived by hand — exactly where an off-by-one range surfaces in blocks.py's output. Constructed 7 deliberate off-by-one scenarios (end-boundary shift, start-boundary shift, 3-digit rows, bare ranges with no wrapping function, a shift crossing a single-digit-to-double-digit offset) and printed real `detect_blocks()` output for each: every one landed in `near_misses` (rapidfuzz scores 93.0-96.4), never `non_conforming`. But also stress-tested the bucketing itself rather than trusting it: an adversarial minimal token comparison (`R[-9]` vs `R[-10]` alone) scored 72.73 — below the near-miss threshold — proving the `near_misses`/`non_conforming` split is a string-similarity artifact, not a structural guarantee. So the rule inspects *both* lists on every block and independently verifies, by diffing the R1C1 pattern strings (regex over the fully-regular R1C1 output grammar — no need to re-parse formulas, since the AST-derived pattern strings blocks.py already stored are sufficient), that exactly one range endpoint shifted by exactly one row/column with everything else in the formula byte-for-byte identical. Reused literal-in-formula-block's exact two-level severity scheme (same-pattern-both-sides -> `high`, else `medium`) rather than inventing a new member-count-based tier — the "how many block-mates share the expected boundary" signal the prompt suggested is real and used, but surfaced as descriptive evidence in the explanation text (actual cell counts on each side) rather than as an arbitrarily-placed severity cutoff. Verified against a new fixture (`range_boundary_mismatch.xlsx`, a rolling 3-month trailing-sum row with one cell hardcoded to a 2-month window) through the full real pipeline: correctly flagged `high`, with the actual cell counts (2 vs 3) and both bordering block spans named in the explanation.

**Rule engine (range-boundary-mismatch) — bugs/defects encountered and fixed:** None in the implementation. Confirmed the `—` (em dash, U+2014) in one printed explanation rendering as `�` in the terminal was a Windows console codepage display artifact, not a bug in the string itself — checked the actual codepoints (`hex(ord(c))`) before concluding this, rather than assuming a printed glyph was ground truth.

**Rule engine (reference-to-blank) — how it was solved:** Investigated before writing any rule logic, per the same discipline as 6a/6b. Confirmed exactly what `DependencyGraph.is_empty()` checks (no formula and no value, OR referenced-but-absent-from-the-parsed-workbook entirely — both collapse to the same `True`) and confirmed that conflation doesn't matter for this rule's design. The obvious framing — "is the referenced blank cell inside a block's row/column span" — turned out not to fit, since blocks only cluster *formula* cells sharing one pattern, not rows of raw data. The real signal found instead: compare a block's own members against each other via `graph.precedents()`/`is_empty()`. Three constructed scenarios with real printed output: a genuine gap in a data row feeding a trailing-sum block (3 of 7 members came back with a blank precedent, 4 clean — real minority-vs-majority evidence); an isolated one-off formula referencing a spacer cell (zero blocks formed, nothing to compare against, no signal); a block where every member uniformly references the same unfilled shared driver (no minority, nothing anomalous). Only the first case has real supporting evidence — raised this as an explicit decision point rather than picking silently (recorded in `todo.md` as a fallback in case of a context break), and confirmed via direct question: ship v1 narrow, block members only, never isolated references. Building the rule surfaced a real interface gap: `Rule.evaluate()` didn't accept a `DependencyGraph`, which this rule genuinely needs (there's no way to get precedent/empty-cell information from `blocks.py`'s own output without reimplementing `depgraph.py`'s logic). Stopped and asked before changing the shared interface rather than doing it as a drive-by; widened `base.py`'s `Rule.evaluate` to `evaluate(self, sheet_blocks, graph=None)` — confirmed backward compatible by re-running the full suite with *only* that one-line interface change applied, before writing a single line of the new rule (127 passed before, 127 passed after, `literal_in_block.py`/`range_boundary.py` and their tests completely untouched). Initially shipped with hand-built `ParsedWorkbook` scenarios only, no real fixture; `generate_fixtures.py` was extended afterward with `reference_to_blank_gap.xlsx` (the same trailing-3-month-sum shape as `range_boundary_mismatch.xlsx`, but with one month's raw data left genuinely blank instead of a range boundary shifted) so this rule is now also verified end-to-end through the real pipeline, not only against hand-built scenarios — confirmed matching the hand-built case exactly: 7 clean members, 3 affected (`F5`, `G5`, `H5`, all referencing blank `F4`), `high` severity.

**Rule engine (reference-to-blank) — bugs/defects encountered and fixed:** None in the implementation. Two of my own hand-written test scenarios had wrong arithmetic in their comments (claimed a "2 clean, 2 affected" tie that was actually 1 clean vs 3 affected, and a separate case) — caught by printing the real per-member blank-precedent breakdown before trusting the hand count, per this project's standing verification habit; the test assertions themselves were still correct (severity `medium` holds either way), only the comment was wrong, and was corrected. Also hit the same bash double-quote `$`-stripping issue as before while investigating a `$`-anchored scenario (`$H$15` silently corrupted mid-command) — caught immediately from the garbled printed output, fixed by moving the script to a file instead of an inline double-quoted shell string, consistent with how the em-dash/terminal-encoding issue in the previous stage was also verified rather than assumed. When `generate_fixtures.py` was extended afterward with `reference_to_blank_gap.xlsx`, confirmed no existing fixture already covered this shape before adding one — `blank_reference.xlsx` (from stage 4) is a single isolated formula with no block at all, the shape this rule deliberately does *not* flag, not the majority-clean/minority-affected block shape this rule actually targets.

**Rule engine (inconsistent-anchoring) — how it was solved:** Investigated before writing any rule logic, per the same discipline as 6a-6c. Confirmed the obvious structural fact stated in the prompt — r1c1.py already encodes anchoring directly (absolute axis -> unbracketed position, relative axis -> bracketed offset), so different anchoring on the same logical reference already normalizes to a different string and can't cluster into one block — then went further and checked *where* the deviation actually lands, the same way 6b/6c did. Three constructed scenarios (a block holding `$B$1` fixed, one member drifting): dropping both `$` at once landed in `non_conforming` (too large a string change); dropping just one axis's `$` landed in `near_misses`, scoring 88-91 — still a rapidfuzz-score artifact, not a guarantee, so the rule inspects both lists and verifies structurally rather than trusting the bucket, same lesson as range_boundary. Detection works per-axis on R1C1 token structure: a differing token is only accepted as a pure anchoring issue when the relative side, resolved to an absolute position using the *candidate cell's own real address* as origin (the only unambiguous position data available, since a block's shared relative offsets have no single fixed origin), matches the absolute side exactly; anything that resolves to a different position is left alone as a different reference, not an anchoring bug. Explicitly verified this rule and range_boundary are mutually exclusive by construction (one needs the resolved position to match, the other needs it to differ by exactly one) — proved with a real test running both rule classes against both deviation shapes, confirming each fires only on its own shape. Severity reuses the same two-level scheme as every prior rule rather than inventing a new one. Verified against a real fixture (`inconsistent_anchoring.xlsx`, a growth-rate-driven revenue row where one cell lost its column anchor — the direction that actually breaks under real horizontal fill-drag, not the row anchor) through the full pipeline: correctly flagged `high`, naming the col axis, the reference (`B1`), and both bordering block spans.

**Rule engine (inconsistent-anchoring) — bugs/defects encountered and fixed:** None. Confirmed no existing fixture already covered this shape before adding one (only a `$B$1` in an unrelated named-range definition in a different fixture, not an anchoring-drift scenario).

**Synthetic-corruption corpus — how it was solved:** Investigated before writing any generator code, per the same discipline as the four rules. Mapped each of the four built rules to its own injection mechanism by reading how it actually fires (not from the README's own illustrative example list, which was checked and found to cover only 2 of the 4 rules and additionally names a "flip a sign" corruption that none of the four rules can detect — confirmed by checking whether a sign-flip's R1C1 pattern change matches any rule's structural predicate; it doesn't, so it's excluded from the taxonomy rather than included as false coverage). One rule required a non-obvious injection direction: `ReferenceToBlankRule` only ever inspects `block.cells` (real block members), never `non_conforming`/`near_misses` — so editing a formula's own reference (the literal reading of "redirect to blank") would evict it from the block and the rule would never see it; the correct injection instead blanks a raw *data* cell that only some block members' formulas read from, leaving every formula untouched. Reused CONTRIBUTING.md's own Week 4 design note (flagged 2026-08-09, before this stage started) as the ground-truth schema rather than re-deriving one: per-cell `clean` / `injected_bug` / `known_intentional`, the last covering seed literals that are structurally indistinguishable from a real hardcode at Tier 0. Checked the README's exact wording ("take 50 clean models... inject bugs into copies of them") to resolve the scale-and-structure question directly rather than guessing: 50 is the clean-model count, not the corpus-entry count, so authoring 50 independently-different business models was rejected in favor of two structurally distinct, parametrized base shapes (growth-chain row, trailing-window-sum row) varied by formula-run length and injection position — 46 entries generated, covering every rule × both severity paths (interior/edge-adjacency for three rules; clean-vs-affected member count for reference-to-blank) × multiple lengths. Asked directly (not decided silently) whether each corrupted copy should carry exactly one injected bug or several — confirmed: one bug per copy, for unambiguous per-injection precision/recall attribution, same standard as `min_block_size` and the rapidfuzz threshold. Verified end-to-end, not just via the generator's own bookkeeping: `tests/test_generate_corpus.py` builds representative entries directly, runs them through the real pipeline, and asserts the exact set of flagged cells against ground truth (149 passing). Corpus output (`corpus/*.xlsx` + paired `*.ground_truth.json`) is gitignored and regenerated on demand, matching `tests/fixtures/`'s own reproducibility rationale — confirmed via `git ls-files`/`.gitignore`, not assumed.

**Synthetic-corruption corpus — bugs/defects encountered and fixed:** One real bug, caught by the verification tests rather than assumed away: dynamically loading `scripts/generate_corpus.py` via `importlib.util` (the same pattern `conftest.py` already uses for `generate_fixtures.py`) crashed on Python 3.14 with `AttributeError: 'NoneType' object has no attribute '__dict__'` — `@dataclass` field-type resolution looks up `sys.modules[cls.__module__]`, which was never populated because `module_from_spec` doesn't register the module in `sys.modules` on its own; `generate_fixtures.py` never hit this because it has no dataclasses. Fixed by registering the module in `sys.modules` before `exec_module()` in the test file. Separately, one of my own test assertions was wrong, not the generator: `test_anchoring_injection_flags_exactly_the_injected_cell` initially asserted `LiteralInBlockRule` produces zero issues on an anchoring-injected shape, but the growth-chain's seed literal independently fires that rule regardless of what else is injected in the same row — exactly the expected behavior the Week 4 design note already describes. Caught by running the test and reading the real failure output rather than trusting the hand-written assertion; fixed the assertion, not the generator.

**Addendum: cross-block-isolation gap closed.** The original investigation floated 4-5 base shape templates including one to test cross-rule/cross-block interference; only two shapes actually shipped, so no fixture proved that corrupting one block leaves an independent second block on the same sheet untouched. Closed with one additional entry, `cross_block_isolation__literal_in_block` (47 entries total now): a corrupted growth-chain block at row 14 and a clean trailing-sum block at row 30 on one sheet. The clean block deliberately uses the trailing-sum shape rather than a second growth-chain — growth-chain always has a seed-literal neighbor that legitimately fires `literal-in-formula-block` on its own (the Week 4 design note's known case), so a second growth-chain block would leave a caveat on "zero issues"; trailing-sum has no such neighbor and genuinely produces zero issues from every rule. Verified directly: `LiteralInBlockRule` fires exactly `{B14: medium, F14: high}` on the corrupted block and nothing else; `InconsistentAnchoringRule`, `RangeBoundaryRule`, and `ReferenceToBlankRule` all return `[]`, and no issue from any of the four rules names a row-30 cell. `test_cross_block_isolation_corrupting_one_block_never_flags_the_other` asserts this explicitly (150 passing).

---

### Week 4 — Baseline evaluation (Tier 0 only)

| Task | Status | Detail |
|---|:---:|---|
| Evaluation harness | 🟢 | `src/ssmlint/evaluation.py` (reusable scoring/metrics logic) + `scripts/run_evaluation.py` (thin CLI). Runs all four Tier 0 rules against every corpus entry and scores each flagged cell against its entry's ground truth. |
| Precision/recall/precision@10 per bug class | 🟢 | Scored per `(rule_id, severity)` plus rolled-up per-`rule_id` totals, exactly per the design note below. Real run against the full 47-entry corpus: **1.000 precision / 1.000 recall** for all four rules (48 TP, 0 FP, 0 FN total) — see the "real numbers" narrative below for why this is expected, not inflated. |

**Design note flagged before this week started (2026-08-09):** `literal-in-formula-block` currently gives every match the same `rule_id`; a seed/anchor literal at the start of a growth chain (e.g. `B14` in `revenue_row_with_hardcode.xlsx`, the value a `=prev*(1+growth)` chain grows from) and a genuine mid-chain hardcode bug (e.g. `H14` in the same fixture) are only partially distinguished today, by `severity` (`medium` vs `high`) — `severity` alone doesn't separate a seed from other one-sided isolated hardcodes within the `medium` bucket. Any synthetic-corruption corpus with realistic growth chains will plant a legitimate seed in every clean model, so scoring precision off a flat `rule_id` (or even `rule_id` + `severity`) tally will count every seed as a false positive and drag precision down for a reason unrelated to real detection quality — potentially masking the actual precision on mid-chain bugs, the case that matters. **Do not fix this by adding an "is this a seed" heuristic to the rule itself** — there's no fully clean deterministic signal for it yet, and it wasn't asked for or approved as a judgment call. Instead: the corpus generator already knows exactly what it planted and where, so track ground truth **per cell** (`clean` / `injected_bug` / `known_intentional`, covering seeds), and compute precision against that per-cell ground truth — sliced by `(rule_id, severity)` — rather than against the rule's raw output counts.

**Evaluation harness — how it was solved:** Investigated before writing any harness code, per the same discipline as every rule stage, and reported four findings for sign-off before implementation (per the standing process rule). (1) Re-read the design note's exact wording and determined it supports excluding `known_intentional` flags from precision's numerator/denominator entirely, while still *reporting* them as a separate, explicitly-visible count — not silently dropping them, which would hide real tool behavior from anyone who didn't read the investigation. (2) Confirmed none of the four rules order/rank their output (`evaluate()` returns dict-iteration order, not a confidence order) — the only real signal is each rule's own two-level `severity`, so precision@10 ranks by severity descending with a deterministic `(entry, cell)` tiebreak, computed per `rule_id` by pooling that rule's flags across the whole corpus. (3) Confirmed `(rule_id, severity)` slicing plus a rolled-up per-`rule_id` number, exactly as the design note states; recall and precision@10 are reported only at the rolled-up level, since severity is a property of a raised `Issue`, not of an abstract missed bug — a false negative has no severity to slice by. (4) Ran the rule engine against the corpus's 10 clean-baseline entries and found growth-chain baselines always produce exactly one *expected* `known_intentional` flag (the seed) while trailing-sum baselines produce zero — so the harness reports both a raw flag count (matching the README's blunt "every flag is a false positive" framing) and a `known_intentional`-filtered "unexplained" count, which is the number that actually matters. Split the implementation across `src/ssmlint/evaluation.py` (reusable scoring logic — Week 5's build plan explicitly re-runs "the Week 4 evaluation with the classifier layered on top", so this needs to be importable, not locked inside a script) and a thin `scripts/run_evaluation.py` CLI. The internal-consistency caveat is printed directly by `format_report_text()` itself, not left in a docstring, per explicit instruction that anyone running the harness should see it without having read the investigation conversation.

**Real numbers, run against the full 47-entry corpus:** all four rules score **1.000 precision / 1.000 recall** (`literal-in-formula-block`: TP=11 FP=0 FN=0, 20 `known_intentional` flags excluded; `range-boundary-mismatch`: TP=10 FP=0 FN=0; `reference-to-blank`: TP=17 FP=0 FN=0; `inconsistent-anchoring`: TP=10 FP=0 FN=0). Clean-baseline check: 5 raw flags across 10 baseline entries, all five the expected growth-chain seed (`known_intentional`), 0 unexplained. This is expected, not inflated — every injection in this corpus is a surgical, structurally-exact match to what its target rule's own predicate checks for (verified per-injection in `tests/test_generate_corpus.py`), so a perfect score here proves the corpus generator and the rules agree with each other, not that the rules will hit this precision against messy real-world spreadsheets. The harness prints this caveat itself, directly alongside the numbers.

**Evaluation harness — bugs/defects encountered and fixed:** Two real bugs, both caught by running the harness rather than trusting the code by inspection, per the standing verification discipline. (1) `LiteralInBlockRule.evaluate()` and `RangeBoundaryRule.evaluate()` don't accept a `graph` parameter at all — confirmed by grepping the actual current signatures rather than assuming every rule matches the `Rule` ABC's optional-graph shape — so calling all four rules uniformly with `graph=graph` would have raised `TypeError` on two of them. Fixed by introspecting each rule's `evaluate` signature (`inspect.signature`) and only passing `graph` when the rule actually accepts it, so this keeps working if Week 5 adds rules with different signatures. (2) The first working version of precision@10 pooled *all* outcomes (including `known_intentional`) before ranking and filtered afterward — smoke-testing surfaced `literal-in-formula-block` scoring `precision@10 (k=5)` instead of `k=10`, traced to 20 `known_intentional` flags (mostly named `clean__growth_chain__*`) sorting alphabetically ahead of the real `literal_in_block__*` true positives and crowding them out of the top-10 window on tiebreak alone. Fixed by excluding `known_intentional` from the ranking pool itself, not just from the final precision count; a regression test (`test_precision_at_10_pool_excludes_known_intentional_entries`) locks the fix in against the real corpus numbers.

---

### Week 5 — Trained classifier (Tier 1)

| Task | Status | Detail |
|---|:---:|---|
| Label generation from synthetic corruption | 🟢 | `src/ssmlint/labeling.py` + `scripts/run_labeling.py`. Generates block-level 4-way labels from the existing 3-state cell ground truth, reusing Week 4's own rule-calling logic. Deterministic entry-level train/val/test split included. |
| Small encoder classifier | 🟢 | `src/ssmlint/classifier.py` + `scripts/train_classifier.py`. `distilbert-base-uncased` (not DeBERTa-v3-small, see 2026-08-18 note below), class-weighted fine-tune, real checkpoint at `models/tier1/` (gitignored). Trained deliberately on all four labels despite `intentional_override`/`unknown` being below the 50-per-class floor — real per-class results reported honestly, including the classes it's weak or fails on. |
| Tier 0 → Tier 0+1 comparison | 🟢 | `classifier.evaluate_tier0_plus_1()` + `ablation.py`'s `tier1_checkpoint_dir` wiring + `scripts/run_ablation.py --tier1-checkpoint`. Real, measured Tier 0+1 row, scored on the checkpoint's own held-out test split, printed alongside Tier 0 re-scored on that same split (never the inflated full-corpus number) for a fair comparison. |

**Design note (2026-08-11): why the classifier is blocked, not skipped.** Investigated before writing any training code, per the standing discipline, and reported five findings for sign-off before implementation. (1) The corpus's cell-level ground truth is 3-state; the classifier's target is a 4-state block-level label. Only `injected_bug → suspected_error` and `known_intentional → intentional_override` have any real mapping — `subtotal` and `unknown` have zero examples anywhere, confirmed directly, because nothing in `generate_corpus.py` was built to represent a legitimate subtotal row or a genuinely ambiguous case. (2) Real volume, run against the full corpus: 61 total blocks, but only **2 distinct R1C1 formula patterns across the entire corpus** — every block is one of exactly two shapes. Building the actual label-generation pipeline (not just estimating) surfaced an even thinner number than the raw ground-truth cell count suggested: a block bordering both a seed AND a flagged bug resolves to `suspected_error` (the real bug takes precedence), so real `intentional_override` examples come only from the 5 uncorrupted growth-chain clean baselines — 5, not the ~20-26 a per-cell count implies. Real per-label counts: `subtotal`=0, `unknown`=0, `intentional_override`=5, `suspected_error`=50 — against even a conservative 50-per-class floor, only one of four labels clears it. (3) Confirmed via `parser.py`/`blocks.py` directly: neither captures row/column header text, so the README's `row_label`/`col_labels` block-description fields cannot be produced without a new pipeline capability — a separate prerequisite, not built here; `BlockExample` carries both fields as explicit `None` rather than omitting or fabricating them. (4) A real train/test split is required before any training happens (never let the classifier or its labels touch the test partition), and Week 4's existing full-corpus 1.0/1.0 result should NOT be reused as the Tier 0 baseline for the eventual Tier 0 vs. Tier 0+1 comparison — that comparison needs Tier 0 re-evaluated on the same held-out test slice for a fair comparison. (5) Realistic dependency footprint reported (CPU-only `torch` + `transformers` + one ~500-600MB model checkpoint, ~1.5-2.5GB total) but nothing installed. **Decision, made directly with the user rather than picked silently:** build the label-generation and training-readiness plumbing now, validated against the existing corpus purely as a correctness check — mirroring how Week 4 caveated its 1.0/1.0 result as pipeline-consistency, not real-world performance — and treat corpus expansion (subtotal/unknown examples, more base shapes, more volume) as its own explicitly-scoped follow-up task before any real training run or ML dependency gets installed.

**Follow-up decision (2026-08-16): Week 5 stops here for now, deliberately.** With the plumbing done and the classifier blocked as above, asked the user directly (not assumed) whether to scope corpus expansion now — as its own dedicated, investigation-first task — or move on to Week 6 (ablation table framing + HTML report builder) with Tier 0 alone and come back to real Tier 1 training later. The user chose Week 6. Corpus expansion and real classifier training are **deferred, not abandoned** — the README itself treats Tier 1/2 as optional layers on top of a Tier 0 that stands on its own, so this is a legitimate stopping point, not a stalled stage. The two red rows above will stay red until corpus expansion is explicitly scoped and done as its own task.

**Corpus expansion (2026-08-17): the deferred prerequisite task, done.** Planned via Claude Code's plan mode (direct investigation of `labeling.py`/`generate_corpus.py`/`evaluation.py`, four judgment calls confirmed via AskUserQuestion before implementation — schema approach, `unknown`'s construction, whether to add a third base shape now, and whether to touch the completed-stage `evaluation.py`). `CellGroundTruth.state` (`scripts/generate_corpus.py`) widened from 3 values to 5, adding `subtotal` and `ambiguous`. Three new generator pieces:
- **`subtotal`**: a new category-total base shape (category rows of raw data + one `SUM`-across-categories "Total" row) — verified empirically, not assumed, that no Tier 0 rule fires on it. Parametrized (cat_count 2-6 × col_count 3-14) to exactly 60 entries.
- **`unknown`**: weak-evidence injections carrying a new `ambiguous` cell state — Tier 0 still flags these deterministically (it has no concept of ambiguity), only the ground-truth label differs from `suspected_error`. Two sources: (a) `n=4`/edge growth-chain and variance-shape injections — verified empirically that `n=3`/edge produces no block at all (below `blocks.py`'s `min_block_size=3`), so `n=4` is the actual shortest flaggable length, meaning it necessarily shares its exact feature signature with some existing `suspected_error` entries — accepted directly with the user as a defensible reflection of real decision-boundary ambiguity, not a flaw, as long as the bulk of each label's distribution (longer/interior cases) stays separable; (b) a near-50/50 affected/clean split on `reference-to-blank`, swept programmatically across `(m, blank_idx)` rather than hand-picked, which surfaced a real structural ceiling (the rule's 3-month window caps `affected` at 3 regardless of row length, so a near-tie ratio is only reachable at short rows) — reported honestly to the user rather than padded with cosmetic-only duplicate entries once that ceiling was found.
- **Variance-row base shape** (pattern diversity): `=Actual-Budget`, a genuinely different two-precedent R1C1 pattern from growth-chain (single relative precedent) and trailing-sum (`SUM` over a range) — distinct formula patterns across the corpus went from 2 to **8**. Hosts its own `literal-in-formula-block`/`reference-to-blank`/`unknown` examples.

`labeling.py`'s `label_for_block` gained two new branches (`subtotal` checked first — the most direct signal; `unknown` checked after `suspected_error`, same real-bug-wins precedence already established for `intentional_override`). `evaluation.py` gained a 4th scoring outcome, `ambiguous_excluded`, handled exactly like `known_intentional` (excluded from precision, reported as its own visible count) — without it, Week 4's precision numbers would have silently dropped for reasons unrelated to rule quality the moment `ambiguous` cells existed, since Tier 0 flags them like any other bug. This was confirmed as in-scope with the user before touching `evaluation.py` (a completed Week 4 file).

**Real numbers, corpus grown from 47 to 144 entries (`scripts/run_labeling.py`):** `subtotal`=60 READY, `suspected_error`=67 READY (up from 50, boosted by the variance shape), `unknown`=20 NOT READY, `intentional_override`=5 NOT READY (unchanged — out of scope for this pass). 162 total blocks (up from 61), 10 unlabeled, 8 distinct formula patterns (up from 2). Overall verdict: still `NOT READY for a real fine-tune` — an honest, expected result, not a bug. `scripts/run_evaluation.py` confirms zero regressions on the original 47 entries: all four rules still 1.000/1.000/1.000, 0 unexplained false positives across all 94 clean-baseline entries (up from 10, since `subtotal`/`ambiguous`-only entries have no `injected_bug` cell and so correctly count as clean baselines too under `is_clean_baseline`'s unchanged definition). 206 tests passing (up from 196).

**Tier 1 classifier build (2026-08-18, in progress) — step-by-step log.** Planned via Claude Code's plan mode again (four judgment calls confirmed before implementation: train now on all four labels and report weak classes honestly rather than deferring further; install `torch`/`transformers`; widen `evaluation.run_evaluation()` with an `entry_names` filter for a fair held-out-test-split Tier 0 baseline rather than a separate non-invasive wrapper; defer live single-workbook CLI/report integration to its own follow-up). Steps landed so far, each committed and pushed separately:

1. **Dependency setup.** New `[project.optional-dependencies] classifier` extra in `pyproject.toml`. Installing `torch`/`transformers` surfaced a chain of real, necessary transitive requirements not anticipated in the plan, added one at a time as each was hit: `sentencepiece` (DeBERTa-v3's tokenizer — later removed again, see step 4), `protobuf` (the real supported SentencePiece-to-fast-tokenizer conversion path — transformers' fallback `tiktoken` path also failed without it), `accelerate>=1.1.0` (`Trainer` hard-requires it in this transformers version). Installed CPU-only `torch` wheels explicitly (`--index-url https://download.pytorch.org/whl/cpu`) to avoid pulling multi-GB CUDA builds on a machine with no dedicated GPU.
2. **`evaluation.py` widening.** `run_evaluation()` gained an optional `entry_names: set[str] | None` filter, confirmed with the user beforehand since it touches a completed Week 4 file. `None` preserves the exact existing whole-corpus behavior (regression-tested). 207 tests passing.
3. **`classifier.py`'s non-training logic**, built and unit-tested before any real model touched it (per standing discipline): `serialize_block_example()` (deterministic text template — no cell values or row/col labels ever appear, since `BlockExample` never carries them), `apply_tier1()` (Tier 0 issue suppression by predicted block label). Two small promotions out of `labeling.py` to support this: `candidate_cells(block)` (out of `label_for_block`'s inline logic) and `build_block_example()` (out of `generate_labeled_examples`'s inline construction), so the same cell-scope and input representation work for arbitrary new (unlabeled) blocks at prediction time, not just corpus entries with known labels. 219 tests passing, zero wall-clock cost added (no torch import needed for any of this — deliberately lazy-imported only inside the training functions, so the base Tier-0-only install never needs it).
4. **Real training run — model choice reversed under real testing.** The plan named `microsoft/deberta-v3-small` as "safer/more compatible for CPU." Wrong in practice: it never completed a single forward+backward step in over 15 minutes of wall-clock time (confirmed still actively burning real CPU the whole time via direct process CPU-time tracking, not hung). Isolated the cause by running the identical loop with `distilbert-base-uncased` (a comparable 66M-param size class): ~1.1s/step, real and measured. DeBERTa-v2/v3's disentangled-attention architecture is a known-bad case for plain CPU eager-mode PyTorch. Reported this finding with real numbers and switched the default model to `distilbert-base-uncased` with the user's direct confirmation rather than silently swapping it. `sentencepiece`/`protobuf` were DeBERTa-v3-tokenizer-specific and are no longer needed for the classifier extra as a result.

**Real training run, completed (`scripts/train_classifier.py`, 2026-08-18).** 152 labeled blocks split 94/20/21 entries (train/val/test, entry-level per `split_corpus_entries`, seed=42) → 65 training steps over 5 epochs, class-weighted cross-entropy (inverse train-label-frequency, mitigating the real 60s/5-20 label imbalance without fabricating examples), ~10m16s real wall-clock on CPU. Checkpoint saved to `models/tier1/` (gitignored, not committed, mirrors `corpus/`'s convention).

**Real held-out test-split classification report (24 examples, never touched during training):**

| Label | Support | Precision | Recall | F1 |
|---|---:|---:|---:|---:|
| `subtotal` | 7 | 1.000 | 1.000 | 1.000 |
| `intentional_override` | 1 | n/a | 0.000 | n/a |
| `suspected_error` | 12 | 0.818 | 0.750 | 0.783 |
| `unknown` | 4 | 0.500 | 0.750 | 0.600 |

Overall accuracy 0.792. Exactly the honest result the data-readiness decision anticipated: perfect on `subtotal` (ample, structurally clean-cut data), solid-not-great on `suspected_error`, weak-but-real signal on `unknown` (3/4 correctly caught, 1 confused with `suspected_error`), and a complete miss on `intentional_override` — the model predicted `suspected_error` for the single test example, unsurprising with only 3 training examples of that label to learn from. Reported as-is, not smoothed over or re-run until a better-looking split appeared.

5. **Tier 0 vs. Tier 0+1 comparison, wired and measured.** `classifier.evaluate_tier0_plus_1()` reuses `evaluation.py`'s own `score_rule_on_entry`/`count_false_negatives` (not re-derived) to score Tier 0's real issues after `apply_tier1` filtering, on exactly the checkpoint's own recorded test split (`models/tier1/split.json`, written by `train_classifier` so the comparison always uses the same entries the classification report above was scored on, never a freshly re-derived split). `ablation.build_ablation_table()` gained an optional `tier1_checkpoint_dir` parameter (`None` preserves all prior behavior exactly, confirmed via a dedicated regression test) that fills in a real, measured Tier 0+1 row. Caught and fixed one real bug along the way: `format_ablation_table_text()` originally only ever printed a row's `notes` when `status == "not_yet_available"`, so the Tier 0+1 row's fair-comparison context (Tier 0 alone, re-scored on the identical test split) was silently computed and then dropped from the printed output the moment the row became `"measured"` — caught by actually reading the CLI's real printed output, not assumed correct. Fixed to print any row's non-empty notes.

**Real ablation comparison (`scripts/run_ablation.py --tier1-checkpoint models/tier1`), as of the split-discrepancy fix below:**

```
Configuration                      Precision    Recall  Precision@10    Cost   Median runtime/workbook
Tier 0 (rules only)                    1.000     1.000         1.000      $0                    4.6 ms
Tier 0 + 1 (+ trained classifier)      1.000     1.000         1.000      $0                       n/a
Tier 0 + 1 + 2 (+ local LLM)             n/a       n/a           n/a      $0                       n/a

[Tier 0 + 1 (+ trained classifier)] Measured on the 21-entry held-out test split from models\tier1.
Tier 0 ALONE on this same test split (the fair comparison point, not the full-corpus row above):
precision=1.0, recall=1.0. CAVEAT: intentional_override had only 1 held-out test example(s) and
0.000 recall in this checkpoint's own classification report -- too thin to provide real evidence the
classifier can distinguish a legitimate seed/override from an actual bug, which is the specific
judgment call Tier 1 exists for per the README. Read any 1.000/1.000 precision/recall reported
elsewhere the same way Tier 0's own perfect score is read: as internal-consistency on this corpus,
not a capability claim.
```

Tier 0+1 doesn't beat Tier 0 here — both score 1.000/1.000/1.000. This is a real, explainable result, not a bug: Tier 0 alone is already perfect on this 21-entry test split (the same internal-consistency property behind Week 4's own corpus-wide 1.0/1.0 — every injection is a surgical, exact match to its target rule's predicate), so there's no headroom left for Tier 1 to improve precision/recall on this metric. Separately, `known_intentional`/`subtotal`-suppression (Tier 1's actual mechanism for helping) doesn't move precision/recall either way, because those cells were already excluded from the numerator/denominator by `evaluation.py`'s own scoring, not counted as false positives to begin with — Tier 1's real value-add on THIS corpus would show up in the raw/unexplained clean-baseline flag count, not this table, and demonstrating it meaningfully would need messier, less surgically-constructed data than this synthetic corpus provides. Stated plainly rather than left as an unexplained "no improvement."

**Split-discrepancy fix + real intentional_override caveat (2026-08-19).** Assembling a full accounting of this stage's real numbers (at the user's direct request, before treating Tier 1 as closed) surfaced two real, previously undocumented gaps, both fixed at the root rather than just noted:
1. `scripts/run_labeling.py`'s readiness report and `train_classifier`'s actual training split were computed from two *different* entry-name universes (`run_labeling.py` included 9 entries where every block was unlabeled; `train_classifier` filtered those out first) — same `seed=42`, but a different input list means a genuinely different partition. Verified directly: only 3 of the old 21 test entries actually overlapped between the two splits. Fixed by promoting a single `labeling.labeled_entry_names()` function both callers now use — `run_labeling.py`'s own printed output now states this guarantee explicitly, not just this file after the fact. Real effect: the readiness report's test-split counts changed from `subtotal=5, intentional_override=2, suspected_error=10, unknown=3` (20 examples, the WRONG split) to `subtotal=7, intentional_override=1, suspected_error=12, unknown=4` (24 examples) — now identical to the real checkpoint's actual held-out test set.
2. The ablation table's Tier 0+1 row showed 1.000/1.000/1.000 with no caveat, even though `intentional_override` — the specific "legitimate override vs. real bug" judgment the README names as Tier 1's reason to exist — had only 1 held-out test example and 0.000 recall. Fixed by making `train_classifier` unconditionally persist `classification_report.json` per checkpoint (previously only written if `--json` was passed at training time; the real `models/tier1` checkpoint didn't have it, so it was backfilled once via inference-only prediction against its recorded test split — no retraining) and adding `classifier.describe_intentional_override_confidence()`, which reads that file and builds the caveat above dynamically — never a hardcoded number that would go stale. The same function is reused by `report.py`'s live single-workbook caveat (see below), so both surfaces describe this checkpoint's real confidence from one source of truth.

229 tests passing after this fix (was 223). `scripts/run_labeling.py`'s readiness report, `scripts/train_classifier.py`'s classification report, and `scripts/run_ablation.py --tier1-checkpoint`'s comparison table are all real, independently re-runnable evidence — none of the numbers above are assumed or interpolated.

**Corpus expansion — bugs/defects encountered and fixed:** None in the shipped logic, but two premises in the initially-approved plan turned out wrong under real testing, caught before generating the full parametrized set rather than after: (1) the plan's `unknown` construction assumed `n=3`/edge was the minimum viable weak-evidence shape; real testing showed `n=3`/edge leaves only 2 clean cells, one short of `blocks.py`'s `min_block_size=3`, so no block forms and nothing is flagged at all — corrected to `n=4`, which surfaced the feature-overlap-with-suspected_error question resolved with the user above. (2) The plan's 50-70 volume target for `unknown` assumed the reference-to-blank near-tie axis would scale with row length; sweeping the real `(m, blank_idx)` space programmatically showed `affected` is capped at 3 by the rule's fixed 3-month window regardless of row length, so a near-tie ratio is only reachable at `n∈{4,5,6,7}` — a hard ceiling, not a coverage gap a wider sweep could close. Both were verified by actually running the generator and rule engine, not derived by hand, and both were reported to the user with real numbers before deciding how to proceed, per this project's standing verification discipline.

**Label generation — how it was solved:** `generate_labeled_examples` runs the real Tier 0 rule engine (reusing `evaluation.py`'s own graph-aware rule-calling logic, promoted from a private `_evaluate_rule` to a public `evaluate_rule` specifically for this reuse) against every corpus entry and builds one `BlockExample` per detected block. `label_for_block` checks, in order, whether any candidate cell (block members + non_conforming neighbors + near-misses) is a ground-truth `injected_bug` the rule engine actually flagged (`suspected_error`), then whether any candidate cell is `known_intentional` (`intentional_override`); a block matching neither stays `label=None` rather than being force-fit into one of the four labels. `split_corpus_entries` splits at the corpus-entry level, never below it — every block in one entry shares that entry's one formula pattern, so a block-level split would leak near-duplicate examples across train/test. Fixed-seed `random.Random`, sorted input first, for full reproducibility. `check_training_readiness` compares real per-label counts (both overall and per-split) against a stated, conservative 50-per-class floor and reports `ready=False` honestly rather than a passing number — verified directly: real full-corpus run produces exactly `{subtotal: 0, intentional_override: 5, suspected_error: 50, unknown: 0}`, 6 blocks with no label at all, split sizes 33/7/7 across the 47 entries.

**Label generation — bugs/defects encountered and fixed:** None in the shipped logic, but one real design gap found and fixed during smoke-testing, not just described afterward: an early version called each rule with `rule.evaluate(sheet_blocks, graph=graph)` uniformly, which would have crashed on `LiteralInBlockRule`/`RangeBoundaryRule` (they don't accept a `graph` parameter at all) — this was actually the same bug already fixed once in `evaluation.py` during Week 4; reusing `evaluation.py`'s `evaluate_rule()` (via the rename above) instead of re-deriving the call logic in `labeling.py` avoided reintroducing it a second time, which is the whole reason `evaluate_rule` was promoted to a public function rather than duplicated.

---

### Week 6 — Local LLM (Tier 2) + ablation table + report UI

| Task | Status | Detail |
|---|:---:|---|
| Local LLM adjudicator | 🔴 | Explicitly deferred, not started — see design note below. Tier 1/2 remain blocked on the Week 5 corpus-expansion prerequisite, which the user chose to defer in favor of shipping Tier 0's report/ablation deliverables first. |
| Full ablation table | 🟢 | `src/ssmlint/ablation.py` + `scripts/run_ablation.py`. Micro-averages Week 4's per-rule precision/recall/precision@10 into one Tier-0 row (README's [Configuration, Precision, Recall, Precision@10, Cost, Median runtime/workbook] shape), plus new full-pipeline runtime instrumentation. Tier 0+1/0+1+2 rows structurally present, `status="not_yet_available"`, `cost="$0"` (an architectural fact, not a measurement). Real result: Tier 0 scores **1.000/1.000/1.000**, median 3.4ms/workbook. |
| HTML report builder | 🟢 | `src/ssmlint/report.py` + `src/ssmlint/templates/report.html.jinja` + new `ssmlint report <path>` CLI subcommand. Runs the real Tier 0 pipeline against one workbook, ranks issues (severity desc + `(rule_id, cell)` tiebreak), builds a per-sheet heatmap (bounding box over flagged cells), and renders a single self-contained HTML file (client-side severity filtering, click-through from heatmap to issue rows) plus a parallel JSON output. Optional `--tier1-checkpoint` flag (2026-08-20) layers live Tier 1 inference on top — see the step-by-step log below. |

**Design note (2026-08-16): why the Local LLM row stays red.** This week's scope was explicitly confirmed with the user before implementation: build the ablation table and report builder on top of the already-working Tier 0 engine, and leave Tier 1 (blocked since Week 5 — the corpus can't yet support a real 4-way fine-tune) and Tier 2 (which depends on Tier 1 existing) deferred rather than started as a drive-by. Four judgment calls were surfaced and confirmed directly before any code was written (recorded in `diya.md`, the saved plan): add `jinja2` as a new dependency (yes — pure-Python, ~134KB wheel, one tiny transitive dep); wrap `Issue` in a new `RankedIssue` rather than widening the completed-stage `rules/base.py` (confirmed — zero risk to a finished stage, matches `evaluation.py`'s own `ScoredIssue` wrapper precedent); show `"$0"` cost on the two not-yet-built ablation rows since it's a stated architectural fact independent of measurement, while every genuinely-measured field on those rows stays `None`; and put the report command in `cli.py` as `ssmlint report` (not a `scripts/run_*.py` script) since it's a single-workbook, end-user-facing operation like `dump`, not corpus-facing dev tooling.

**Ablation table — how it was solved:** Reuses Week 4's scoring logic entirely rather than duplicating it — `build_ablation_table()` calls `evaluation.run_evaluation()` as-is and reads only `RuleRollup`'s already-public fields to compute one micro-averaged Tier-0 row (`sum(tp)/sum(tp+fp)` for precision, same shape for recall; precision@10 reconstructed as a weighted average of each rule's own top-10 result, documented explicitly in the module docstring as an approximation of a true cross-rule pooled ranking, since no such pooled signal exists). Runtime instrumentation is genuinely new — nothing timed any part of this pipeline before. Confirmed directly with the user, and stated verbatim in `ablation.py`'s docstring: each timed sample covers the full pipeline (fresh `parse_workbook` → `build_graph` → `detect_blocks`, which itself performs per-cell tokenization/AST/R1C1 normalization → all four rule evaluations), not just rule evaluation against pre-built blocks, with no cached state reused across corpus entries. Verified against the real 47-entry corpus: Tier 0 row exactly matches Week 4's own already-verified per-rule totals (TP 11/10/17/10, all FP/FN zero) rolled up to 1.000/1.000/1.000.

**Ablation table — bugs/defects encountered and fixed:** None. The only real design decision (not a bug) was how to reconstruct `tp_in_top_k` from `RuleRollup`'s stored `precision_at_10` ratio and `precision_at_10_k` count without adding a new field to a completed-stage-adjacent file — `round(precision_at_10 * precision_at_10_k)` is exact for these small integer values, confirmed by a unit test using the real verified per-rule totals rather than assumed.

**HTML report builder — how it was solved:** `build_report()` runs the real pipeline (`parse_workbook` → `build_graph` → `detect_blocks` → all four rules) against one workbook — a genuinely different consumer than `evaluation.py`/`ablation.py`, which score against the synthetic corpus's ground truth; a real report has no ground truth at all. Ranking is explicitly adapted, not reinvented, from `evaluation.py`'s severity-desc + deterministic-tiebreak convention (documented in the module docstring) — the tiebreak changes from `evaluation.py`'s `(entry, cell)` to `(rule_id, cell)` since a single-workbook report has no corpus-entry axis. The sheet heatmap is a bounding box computed only from flagged-cell addresses, not a sheet's full used range — a deliberate v1 scoping choice (documented as a known limitation, same as prior stages' documented scope boundaries) to avoid rendering a huge, mostly-empty grid for a large real workbook. The Jinja2 template uses `autoescape=True` since issue explanation/suggested-fix text ultimately derives from formula strings and must never be trusted as safe HTML — confirmed with a deliberate test injecting `<script>alert(1)</script>` into an explanation and asserting the raw tag never appears unescaped in the rendered output. `[tool.setuptools.package-data]` was added to `pyproject.toml` so the `.jinja` template ships inside real installed wheels, not just the editable dev install — verified directly by building a real wheel with `pip wheel` and confirming `ssmlint/templates/report.html.jinja` is actually present inside it, not assumed from the config alone. Verified end-to-end against the real `revenue_row_with_hardcode.xlsx` fixture (already known-flagged: `H14` high, `B14` medium): ranked order, heatmap bounding box, and both HTML and JSON caveat fields all confirmed via real output, not a hand-built stub.

**HTML report builder — bugs/defects encountered and fixed:** None in the implementation. One honest limitation surfaced and disclosed rather than glossed over: no `chromium-cli` or Playwright is available in this environment, so the generated HTML could not be screenshotted and visually inspected by the agent itself — the real default browser was opened directly on the actual rendered file for human review instead, and the rendered markup itself was read and checked by hand (confirming, e.g., that `autoescape` correctly rendered `cell's` as `cell&#39;s`) as the strongest verification actually available, on top of the automated structural/escaping tests.

---

**Live Tier 1 inference in `ssmlint report` (2026-08-20) — step-by-step log.** Until now Tier 1 only ever ran offline against the synthetic corpus (`evaluate_tier0_plus_1`), where every block's `base_shape` comes from `ground_truth.json`. This wires it into the real `ssmlint report` CLI, so a real, standalone workbook actually gets Tier 1-reviewed. Planned via Claude Code's plan mode; a real blocker was found and resolved with the user before implementation, then two more real gaps were found and fixed as their own first step (Deliverable 0) before the live-inference work itself began, per direct instruction to fix them at the root rather than leave them documented-only — see the split-discrepancy/intentional_override-caveat entry above. Steps landed, each committed and pushed separately:

1. **The blocker and how it was resolved.** `build_block_example` requires a `base_shape` (one of 5 corpus archetypes), but that value only ever exists because `scripts/generate_corpus.py` writes it into synthetic `ground_truth.json` at generation time — confirmed directly that no derivation logic exists anywhere for a real workbook. Rather than build a real trained shape-classifier or use a placeholder constant, the user chose a lightweight regex heuristic. `labeling.guess_base_shape()` matches a block's own R1C1 `pattern` against the 4 real per-block archetypes (`growth_chain`, `trailing_sum`, `category_total`, `variance` — `cross_block_isolation` is corpus-only, never a real per-block shape), verified against both the corpus's absolute-ref growth-chain form AND the real (non-synthetic) `revenue_row_with_hardcode.xlsx` fixture's relative-ref form, so the regex isn't overfit to synthetic data. Returns `"unrecognized"` honestly for anything else, documented explicitly as a heuristic approximation, not the corpus's ground-truth rigor. 236 tests passing.
2. **`apply_tier1_to_workbook`** (`classifier.py`) composes `load_classifier` + `predict_labels` + `apply_tier1` for one real, already-parsed workbook, using `guess_base_shape` in place of ground truth. Returns both surviving AND suppressed issues (unlike `apply_tier1` itself), since a real report needs to show what Tier 1 did, not silently drop it. 239 tests passing.
3. **`report.py` wiring.** New optional `build_report(..., tier1_checkpoint=None)` param; `None` preserves the exact prior Tier-0-only behavior (regression-tested against the existing real-fixture test). New `WorkbookReport.suppressed_issues` field — issues Tier 1 suppresses move to their own section rather than vanishing, matching the project's own `REPORT_CAVEAT` ethos ("every flagged cell here deserves a human look, not automatic trust either way"). New `REPORT_CAVEAT_TIER1` explains the base_shape-heuristic gap; combined at build time with `classifier.describe_intentional_override_confidence()`'s real, dynamic numbers — the same function `ablation.py` uses — so a live report and the offline ablation table never tell two different stories about how much to trust Tier 1. A bad/missing checkpoint fails loudly (wrapped `RuntimeError`), never silently falls back to Tier 0 unmarked, matching the CLI's existing all-errors-crash convention. 244 tests passing.
4. **Template + CLI.** `report.html.jinja` gained a visually-muted "Suppressed by Tier 1" section (its own Sheet column, since it isn't scoped to one sheet's heatmap) plus a summary-line count, both shown only when non-empty. `cli.py` gained `--tier1-checkpoint PATH`, threaded straight to `build_report`; stdout gets a `", N suppressed by Tier 1"` suffix when something was actually suppressed. 245 tests passing.

**Real verification, run against `tests/fixtures/revenue_row_with_hardcode.xlsx` with the real trained checkpoint at `models/tier1` (not mocked):**

```
# without --tier1-checkpoint
Wrote HTML report to ...\no_tier1.html (2 issue(s) found)

# with --tier1-checkpoint models/tier1
Wrote HTML report to ...\with_tier1.html (2 issue(s) found)
```

Both `H14` (the real hardcode bug) and `B14` (the growth-chain seed, `known_intentional` by the corpus's own convention) survived Tier 1 review unsuppressed — the real classifier did not predict `subtotal`/`intentional_override` for either block on this real, non-synthetic fixture. This is reported as-is, not as a failure: it's the honest, expected consequence of `describe_intentional_override_confidence`'s own caveat (1 held-out test example, 0.000 recall for exactly this judgment call) showing up in a live report, not just an offline table — the real JSON caveat field reads:

> "...CAVEAT: intentional_override had only 1 held-out test example(s) and 0.000 recall in this checkpoint's own classification report -- too thin to provide real evidence the classifier can distinguish a legitimate seed/override from an actual bug, which is the specific judgment call Tier 1 exists for per the README..."

Surviving issues carry `tier: "Tier 0 (Tier 1-reviewed)"`; `suppressed_issues` is correctly empty in both the JSON and the rendered HTML (the "Suppressed by Tier 1" section is correctly omitted, confirmed by grepping the real rendered file for zero occurrences).

**Live Tier 1 inference — bugs/defects encountered and fixed:** None during this step's own implementation. The two real bugs relevant to this stage (the split discrepancy and the missing intentional_override caveat) were found and fixed as their own prerequisite step (Deliverable 0, documented above) before this work began, precisely so this step wouldn't need to redo that fix later.

---

## Continuous Integration (2026-08-20)

`.github/workflows/tests.yml` runs the full suite on every push and pull request to `main`. Matrix: `{ubuntu-latest, windows-latest} × {3.10, 3.12}` (4 jobs, `fail-fast: false`) — Windows included since the project is actually developed on Windows and had never run CI there before; 3.10 tests `pyproject.toml`'s stated `requires-python` floor, 3.12 a recent stable version. Only the `dev` extra (`pytest`) is installed, not `classifier` — confirmed by grep before committing that no top-level `torch`/`transformers` import exists anywhere in `src/`, and every real-checkpoint call site in the test suite (`load_classifier`/`predict_labels`/`evaluate_tier0_plus_1`/`apply_tier1_to_workbook`) is mocked, so the full 245-test suite genuinely passes without the ~1.5-2.5GB ML dependency installed.

**Real bug found and fixed by CI's first-ever run, not assumed away:** the first push (`32345784732`) failed all 4 matrix cells, 17 tests each, all one root cause — `corpus/` is gitignored and was only ever generated by hand, once, long ago, then left on disk locally; nothing in the test suite actually guaranteed its existence, unlike `tests/fixtures/` (regenerated every session by `conftest.py`'s `_build_fixtures` autouse fixture). This was invisible on every local run since the corpus had quietly existed on disk for the project's entire history — a genuinely fresh checkout (CI's first run) was the first environment to ever expose it. Fixed by adding the equivalent `_build_corpus` autouse fixture to `conftest.py`, which hit the same `@dataclass`/`sys.modules` module-loading bug already documented once in `tests/test_generate_corpus.py` (Week 3) — fixed the same way, registering the dynamically-loaded module in `sys.modules` before `exec_module()` runs. Verified for real, not just by re-running CI: deleted the local `corpus/` (288 files) to simulate a genuinely fresh checkout, confirmed the full suite passed cold (245/245) and regenerated an identical 288-file corpus.

**Real final passing run** (`gh run view 32347303430`):

```
✓ main Tests · 32347303430
JOBS
✓ test (windows-latest, 3.12) in 1m12s -- 245 passed in 27.01s
✓ test (windows-latest, 3.10) in 1m6s  -- 245 passed in 27.85s
✓ test (ubuntu-latest, 3.10) in 38s    -- 245 passed in 23.63s
✓ test (ubuntu-latest, 3.12) in 37s    -- 245 passed in 21.06s
```

`tests/test_packaging.py`'s real `pip wheel` subprocess test (previously flagged as a real false-pass risk on a stale local build cache) passed cleanly on every job, including the very first failing run — confirmed it was never the source of any CI failure here; the corpus-reproducibility gap was a separate, previously-unknown issue.

---

## row_label / col_labels capture (2026-08-20)

`BlockExample.row_label`/`col_labels` had been `None` on every example since Week 5 — the README's own worked example shows real values (`"row_label": "Revenue - Enterprise"`, `"col_labels": ["Jan-24", "Feb-24", ...]`), but neither `CellRecord` nor `Block` ever captured any header text, only linked one to a block. Investigation confirmed **no parser-level gap**: `parser.py` already captures every literal cell's text (`tests/test_parser.py::test_literal_values` already asserted this); the gap was purely that `blocks.py` had no positional grid to look a header cell up with.

Closed with two fixed-offset heuristics in `blocks.py`, honestly documented as heuristics (same convention as `guess_base_shape`), not a general solution: `row_label` reads column A of the block's own row; `col_labels` reads exactly one row above each spanned column. Both `None`/all-`None` when a real sheet doesn't follow that layout — an honest miss, not a bug. `labeling.build_block_example` now passes these through instead of hardcoding `None`. **Confirmed scope decision:** `classifier.serialize_block_example` is deliberately left unwired — the already-trained Tier 1 checkpoint has never seen row/col label text in its input format, so feeding it new-shaped text now would be an unverified distribution shift, not a strict improvement; wiring it in is a separate, retraining-aware follow-up.

One genuinely stale test found and fixed while implementing, not just noted: `test_full_corpus_labeling_matches_verified_numbers`'s premise was "row_label/col_labels always None" — still true for `row_label` (the real corpus's shape builders write no column-A text anywhere), but `col_labels` is now a real list of `None`s per spanned column, never bare `None`, since `_capture_col_labels` always returns one entry per column regardless of whether anything was found.

**Real captured output**, run against the real (non-synthetic) `revenue_row_with_hardcode.xlsx` fixture:

```
Block Model!C14:G14
  row_label:  'Revenue'
  col_labels: ['Feb-24', 'Mar-24', 'Apr-24', 'May-24', 'Jun-24']
Block Model!I14:M14
  row_label:  'Revenue'
  col_labels: ['Aug-24', 'Sep-24', 'Oct-24', 'Nov-24', 'Dec-24']
```

251 tests passing (up from 245).
